package org.example;

import org.example.Static.GameSetup;

public class Main {
    public static void main(String[] args) {
        GameSetup.showHelloMessage(); // приветственное сообщение
        GameSetup.parseInput(); // обрабатываем ввод
    }
}